{
  "name": "onmyradar-www",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "npm run build && vitest run",
    "test:production": "SITE_ENV=production npm run test"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "vitest": "5.0.0"
  }
}
