{
  "name": "onmyradar-www",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "vitest": "^5.0.0"
  }
}
